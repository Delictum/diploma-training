program Est;

var
  a, b, c, s: integer;

function Sum(x, y, z, sm: integer): integer;

begin
  sm := x + y + z;
  write(sm);
end;

begin
  write('Введите А: ');
  readln(A);
  write('Введите B: ');
  readln(B);
  write('Введите C: ');
  readln(C);
  sum(a, b, c, s);
end.